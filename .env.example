# GitHub Personal Access Token with repo:issues scope
GITHUB_TOKEN=

# Comma-separated list of repositories to monitor (format: owner/repo)
# Example: octocat/hello-world,github/docs
GITHUB_REPOS=

# Label that triggers Claude Code execution (default: implement)
TRIGGER_LABEL=implement

# Optional: Custom cache directory (default: ~/.ccradar)
# CCRADAR_CACHE_DIR=/path/to/cache

# Optional: Claude CLI path (if not in PATH)
# CLAUDE_PATH=/path/to/claude

# Optional: Working directory for Claude execution
# CCRADAR_WORK_DIR=/path/to/work/directory